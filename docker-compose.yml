services:
  viewer:
    build:
      context: .
      dockerfile: Dockerfile
    image: sensor-noise-simulator-viewer:latest
    ports:
      - "5173:5173"
    environment:
      PORT: "5173"
    restart: unless-stopped
